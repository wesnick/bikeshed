{% if manifest['prompts'] | length %}
  <p class="menu-label">Prompts</p>
  <ul class="menu-list">
  {% for key, item in manifest['prompts'].items() %}
      <li>
        <a href="/prompt/{{ key }}" 
           hx-get="/prompt/{{ key }}" 
           hx-target="#dashboard">
          {{ item.name }}
        </a>
        <span class="is-small">{{ item.description }}</span>
      </li>
  {% endfor %}
  </ul>
{% endif %}
