{% if tags %}
  {% for tag in tags %}
    <a href="#"
       class="dropdown-item tag-suggestion"
       data-tag-id="{{ tag.id }}"
       data-tag-name="{{ tag.name }}">{{ tag.name }} ({{ tag.id }})</a>
  {% endfor %}
{% else %}
  <span class="dropdown-item-text">No tags found</span>
{% endif %}
